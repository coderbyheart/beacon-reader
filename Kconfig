menu "Temperature Beacon Reader"

config APP_VERSION
	string "The application version"
	default "0.0.0-development"

endmenu # Temperature Beacon Reader

menu "Zephyr Kernel"
source "$ZEPHYR_BASE/Kconfig.zephyr"
endmenu

module = TBR
module-str = Temperature Beacon Reader
